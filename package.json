{
  "name": "toolshop-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Suite de automatización (Playwright) - Proyecto Final de Testing Funcional",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
